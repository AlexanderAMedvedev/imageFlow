        
        //1. write in the service the new value of Like
        //  call the method with the parameters
        //  make http request (POST/DELETE)
        //  have a look at the data in both cases
        
        //2. write in the local photos the new value of Like
             make the element for the photos array
             change the current element with the new one
             
        //3. change the symbol of like in the cell
